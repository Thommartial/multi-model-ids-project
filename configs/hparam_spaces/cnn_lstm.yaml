# Hyperparameter search space -- CNN-LSTM (Phase 2)
# Method: Optuna (TPE); Budget: 30 trials; Selection target: val macro-F1
# Frozen: 2026-05-26 (Task 2.2)
window_size:    {type: categorical, choices: [16, 32, 64]}
conv_filters:   {type: categorical, choices: [32, 64, 128]}
kernel_size:    {type: categorical, choices: [3, 5, 7]}
lstm_hidden:    {type: categorical, choices: [64, 128, 256]}
dropout:        {type: float,       low: 0.0, high: 0.5}
learning_rate:  {type: float,       low: 1.0e-4, high: 1.0e-2, log: true}
batch_size:     {type: categorical, choices: [128, 256]}
max_epochs: 100
early_stopping_patience: 10
