# ============================================================
# Python dependencies — Multi-Model IDS project
# Target interpreter: Python 3.10  (see environment.yml; proposal Sec. 7)
#
# Pinning strategy
#   ==  exact pin on packages that affect numerical results
#       (the TensorFlow 2.13 scientific stack) — for reproducibility.
#   >=  lower bound on tooling/infrastructure — so the environment
#       installs cleanly across machines. After a successful build,
#       exact resolved versions are frozen into requirements-lock.txt
#       (pip freeze) for full reproducibility — see Task 15.
# ============================================================

# --- Core scientific stack (exact pins — affect results) ---
tensorflow==2.13.0
tensorboard==2.13.0
numpy==1.24.3
pandas==2.0.3
scipy==1.11.1
scikit-learn==1.3.0
xgboost==1.7.6
imbalanced-learn==0.11.0
statsmodels==0.14.0

# --- Hyperparameter optimisation ---
optuna>=3.3,<4.0
hyperopt>=0.2.7
ray[tune]>=2.5,<3.0

# --- Data versioning & validation ---
dvc>=3.0,<4.0
great-expectations>=0.17,<0.18

# --- Experiment tracking ---
mlflow>=2.5,<3.0
wandb>=0.15

# --- Visualisation ---
matplotlib>=3.7,<3.9
seaborn>=0.12,<0.14
plotly>=5.15
scienceplots>=2.1
streamlit>=1.25

# --- Statistical analysis & interpretability ---
scikit-posthocs>=0.7
shap>=0.42,<0.45

# --- Configuration & utilities ---
hydra-core>=1.3,<1.4
pydantic>=2.4,<3.0
pyyaml>=6.0
joblib>=1.3
python-dotenv>=1.0
loguru>=0.7
tqdm>=4.65
psutil>=5.9

# --- Dashboard backend (real-time updates) ---
websockets>=11.0
fastapi>=0.100
uvicorn>=0.23

# --- Notebooks ---
jupyterlab>=4.0
ipykernel>=6.25

# --- Dataset download (optional route) ---
kaggle>=1.5

# --- Testing & code quality ---
pytest>=7.4
pytest-cov>=4.1
black>=23.7
isort>=5.12
mypy>=1.4
pre-commit>=3.3
