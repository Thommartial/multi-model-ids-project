# ============================================================
# Python dependencies — Multi-Model IDS project
# Target interpreter: Python 3.10  (see environment.yml)
#
# Pinning strategy: lower bounds only. This lets the resolver pick a
# mutually-compatible, current set of libraries instead of an
# over-constrained one. After the environment builds successfully,
# the exact resolved versions are frozen into requirements-lock.txt
# (`pip freeze`) — that lock file is the reproducibility artefact
# (see Task 15).
# ============================================================

# --- Core scientific / ML stack ---
tensorflow>=2.16,<2.18
numpy>=1.26
pandas>=2.1
scipy>=1.11
scikit-learn>=1.3
xgboost>=2.0
imbalanced-learn>=0.12
statsmodels>=0.14

# --- Hyperparameter optimisation ---
optuna>=3.5
hyperopt>=0.2.7
ray[tune]>=2.9

# --- Data versioning & validation ---
dvc>=3.30
great-expectations>=0.18

# --- Experiment tracking ---
mlflow>=2.10
wandb>=0.16

# --- Visualisation ---
matplotlib>=3.8
seaborn>=0.13
plotly>=5.18
scienceplots>=2.1
streamlit>=1.30

# --- Statistics & interpretability ---
scikit-posthocs>=0.9
shap>=0.44

# --- Configuration & utilities ---
hydra-core>=1.3
pyyaml>=6.0
joblib>=1.3
python-dotenv>=1.0
loguru>=0.7
tqdm>=4.66
psutil>=5.9

# --- Dashboard backend (real-time updates) ---
websockets>=12.0
fastapi>=0.110
uvicorn>=0.27

# --- Notebooks ---
jupyterlab>=4.0
ipykernel>=6.25

# --- Dataset download (optional route) ---
kaggle>=1.6

# --- Testing & code quality ---
pytest>=8.0
pytest-cov>=4.1
black>=24.0
isort>=5.13
mypy>=1.8
pre-commit>=3.6
